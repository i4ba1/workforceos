import { PagePlaceholder } from '../../shared/ui/PagePlaceholder';

export default function PeoplePage() {
  return (
    <PagePlaceholder
      title="People"
      description="Employee directory, profile and employment assignment will be shown here."
    />
  );
}
