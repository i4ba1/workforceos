import { PagePlaceholder } from '../../shared/ui/PagePlaceholder';

export default function AttendancePage() {
  return (
    <PagePlaceholder
      title="Attendance"
      description="Daily attendance list, record detail, corrections and exceptions will be shown here."
    />
  );
}
