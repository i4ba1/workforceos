import { PagePlaceholder } from '../../shared/ui/PagePlaceholder';

export default function SchedulingPage() {
  return (
    <PagePlaceholder
      title="Schedule"
      description="Schedule calendar, shift templates and assignment flows will be shown here."
    />
  );
}
