import { PagePlaceholder } from '../../shared/ui/PagePlaceholder';

export default function PayrollPage() {
  return (
    <PagePlaceholder
      title="Payroll"
      description="Pay periods, readiness, close/reopen and export history will be shown here."
    />
  );
}
