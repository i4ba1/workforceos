import { PagePlaceholder } from '../../shared/ui/PagePlaceholder';

export default function DashboardPage() {
  return (
    <PagePlaceholder
      title="Dashboard"
      description="Today overview, exceptions and payroll readiness will be shown here."
    />
  );
}
